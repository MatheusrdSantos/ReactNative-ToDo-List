import { StyleSheet } from 'react-native';
/* Task styles */
export default StyleSheet.create({
    container:{
        flex:1,
        flexDirection: 'row',
        marginBottom:10,
    },
    textLineThrough:{
        textDecorationLine: 'line-through'
    },
    description:{
        flex:1,
    }
});